
  - name: Install kea package
    apt: pkg=kea-dhcp4-server


  - name: Create missing directory for lockfile
    file:
      path: /var/run/kea
      state: directory
      owner: root
      group: root
      mode: '0775'


  - name: Kea configuration
    copy:
      src: root/etc/kea/kea-dhcp4.conf
      dest: /etc/kea/kea-dhcp4.conf
    notify:
    - Restart kea-dhcp4-server


  - name: Ensure the DHCP service is started
    service:
      name: kea-dhcp4-server
      state: started
      enabled: yes
