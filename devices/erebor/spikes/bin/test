#!/usr/bin/env bash

set -e

# Vagrant rspec tests don't seem to work in a nix devshell. Given that the
# cucumber tests exercise this module, I'm not too fussed ignoring the tests.
#rspec --fail-fast

cucumber --publish-quiet --no-source
